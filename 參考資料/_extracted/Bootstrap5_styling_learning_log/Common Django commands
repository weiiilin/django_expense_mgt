0. 切換目錄
在 VS Code 內建終端機 (Terminal) 中（ Ctrl + ~ ），確認環境為 (venv_312) ，然後進入專案層級：
cd CH1/MyDjango
請確認該目錄下存有 manage.py 檔案。

# 1. 產生資料庫遷移檔案（當模型有變更時）
python manage.py makemigrations 

# 2. 進行資料庫同步（初次執行必備）
python manage.py migrate

# 3. 啟動開發伺服器
python manage.py runserver

# 4. 預覽結果 開啟瀏覽器並輸入： http://127.0.0.1:8000/

# 常用命令
# 建立專案
django-admin startproject project_name

# 建立新的 Django app
python manage.py startapp app_name

# 建立超級使用者（管理員帳號）
python manage.py createsuperuser

# 進入 Django shell（互動式命令列）
python manage.py shell

# 產生資料庫遷移檔案（當模型有變更時）
python manage.py makemigrations 

# 執行資料庫遷移（將遷移檔案應用到資料庫）
python manage.py migrate

# 顯示目前的資料庫遷移狀態
python manage.py showmigrations

# 收集靜態檔案（部署前必備）
python manage.py collectstatic

# 執行測試
python manage.py test


# 建立計算BMI值網站的提示詞
請修改這個專案，使其提供前端網頁。可以讓使用者輸入身高(cm)及體重(kg)，
按[計算]按鈕後，將資料傳送到後端計算BMI，然後在後端將身高、體重、BMI及
當下的時間存入資料庫中，並將BMI值傳回前端網頁顯示。